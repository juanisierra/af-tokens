# af-tokens
Apex Fusion NFTs


## AF Testnet Ninja Token

This is the original Apex Fusion Testnet Ninja Token, to deploy contract run:
```
truffle migrate --network af_testnet
```

In order to mint tokens, modify the `script/mint.js` file and execute `truffle exec scripts/mint.js --network af_testnet`